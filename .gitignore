# ─── BLOCO DE SEGURANÇA (padrão F:\Claude\SEGURANCA.md) ───
.env
.env.*
!.env.example
*.local
*.pem
*.key
*secret*
*senha*
!*.example

# ─── Estado local (gerado em runtime) ───
data/estado.json
data/estado_*.json
data/usuarios.json
data/campanhas_meta.json
data/bancos_npc.json
data/aventuras.json
data/compras.json

# ─── Firebase (chave de serviço — NUNCA versionar) ───
firebase-key.json
*firebase*adminsdk*.json
serviceAccount*.json

# ─── Comuns ───
__pycache__/
*.pyc
server-out.log
server-err.log
.venv/
venv/
.DS_Store
Thumbs.db

# --- Ferramentas locais do Claude Code (nao fazem parte do produto) ---
.agents/
.claude/skills/
skills-lock.json
.playwright-mcp/

# --- Testes (Node/Playwright) ---
node_modules/
tests/.data-teste/

# icones (trabalho temporario de geracao/recorte)
scratch-icons/

# --- Backups manuais antes de editar (convencao do CLAUDE.md) ---
# Ficam SO em disco: sao a rede de seguranca local, nao fazem parte do produto.
# O historico ate 2026-08-18 segue no Git; daqui pra frente nao entram em commit.
versoes/

# --- Folhas de mockup do Gemini (material bruto, precisam recorte) ---
static/icons/assets/
